{% extends 'base.html.twig' %}

{% block title %}Posts{% endblock %}

{% block body %}
    <h1>Posts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ posts.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ posts.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('posts_index') }}">back to list</a>

    <a href="{{ path('posts_edit', {'id': posts.id}) }}">edit</a>

    {{ include('posts/_delete_form.html.twig') }}
{% endblock %}
