{% extends 'base.html.twig' %}

{% block title %}    {{parent()}} - Edit Post{% endblock %}

{% block body %}
    {{parent()}}
    <h1>Edit Posts</h1>

    {{ include('posts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('posts_index') }}">back to list</a>

    {{ include('posts/_delete_form.html.twig') }}
{% endblock %}
