
{% extends 'base.html.twig' %}

{% block title %}    {{parent()}} - User Form {% endblock %}

{% block body %}
  <div class="row justify-content-md-center">
        <div class="col-8 formFont">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
        
        </div>
    </div>
{% endblock %} 