
{% extends 'base.html.twig' %}

{% block title %}     {{parent()}} - Crear Categorías{% endblock %}

{% block body %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-success">{{ button_label|default('Crear') }}</button>
    <a href="{{ path('category_index') }}" id="colorRed">Cancelar</a>
{{ form_end(form) }}
{% endblock %}
