{% extends 'base.html.twig' %}

{% block title %}    {{parent()}} - Categoría{% endblock %}

{% block body %}
    {{parent()}} 
    <h4>Categoría</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
                <th>Nombre</th>
                <td>{{ category.name }}</td>
                                <th>Nombre</th>
                <td>{{ category.name }}</td>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">Regresar </a>
    /
    <a href="{{ path('category_edit', {'id': category.id}) }}"> Editar</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
